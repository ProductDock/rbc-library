spring:
  cloud:
    gcp:
      sql:
        enabled: false

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://placeholder
          jwk-set-uri: http://placeholder

cors:
  allowed:
    origins: localhost:3000